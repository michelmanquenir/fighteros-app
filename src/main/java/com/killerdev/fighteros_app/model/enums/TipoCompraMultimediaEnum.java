package com.killerdev.fighteros_app.model.enums;

// Coincide con tipo_compra_multimedia_enum (Postgres).
public enum TipoCompraMultimediaEnum {
    foto, video, poster, diseno, paquete, edicion, highlight
}
