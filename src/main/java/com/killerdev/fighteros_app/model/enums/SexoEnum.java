package com.killerdev.fighteros_app.model.enums;

// Los nombres de las constantes deben coincidir exactamente con las etiquetas
// del ENUM nativo de Postgres (sexo_enum) para @JdbcTypeCode(SqlTypes.NAMED_ENUM).
public enum SexoEnum {
    M, F
}
