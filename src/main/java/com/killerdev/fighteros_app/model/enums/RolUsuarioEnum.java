package com.killerdev.fighteros_app.model.enums;

// Coincide con rol_usuario_enum (Postgres). No renombrar a MAYÚSCULAS.
public enum RolUsuarioEnum {
    boxeador, entrenador, gimnasio_admin, organizador,
    arbitro, juez, fotografo, productora, admin, espectador
}
